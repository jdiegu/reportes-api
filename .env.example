PORT=3000
MONGO_URI=mongodb://localhost:27017/reports
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio

# CORS — Separar dominios con coma
# Si esta vacio, acepta todos los origenes (desarrollo)
# En produccion con Nginx proxy, dejar vacio o incluir el dominio:
# CORS_ORIGINS=https://tu-dominio.com,http://tu-dominio.com
CORS_ORIGINS=
